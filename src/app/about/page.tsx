/* /about - the route the site nav and footer already link to. The page
 * itself lives with the other kit pages under explorations/about. */
import AboutPage, { metadata as aboutMetadata } from "../explorations/about/page";

export const metadata = aboutMetadata;
export default AboutPage;
